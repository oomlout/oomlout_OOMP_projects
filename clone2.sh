cd sourceFiles/sparkfun
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=1" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=2" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=3" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=4" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=5" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=6" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=7" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=8" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=9" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=10" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=11" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
sudo curl -s "https://api.github.com/orgs/sparkfun/repos?per_page=100&page=12" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
cd ..
cd adafruit
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=1" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=2" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=3" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=4" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=5" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=6" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=7" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=8" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=9" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=10" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=11" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=12" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=13" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=14" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=15" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
curl -s "https://api.github.com/orgs/adafruit/repos?per_page=100&page=16" | ruby -rrubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["clone_url"]} ]}'
